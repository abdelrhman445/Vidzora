Optional: drop a .ttf/.otf here (Arabic-capable font recommended if your
scripts are in Arabic, e.g. Cairo or Amiri) and point
`SUBTITLE_FONT_PATH` in `.env` at it. Left empty by default, which uses
MoviePy/Pillow's bundled default font.

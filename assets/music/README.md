Drop a royalty-free instrumental track here (e.g. `background.mp3`) and
point `BACKGROUND_MUSIC_PATH` in `.env` at it. Left empty by default —
no copyrighted or third-party audio ships with this repo, so background
music is opt-in and BYO-license.
